# AGENTS.md

このリポジトリは、Goldberg多面体ベースのセルシミュレーションを扱います。

## プロジェクトの不変条件

- 五角形セルはシミュレーションから除外しないでください。欠陥として削除したりスキップしたりしません。
- 各セルは必ず `neighbors`, `neighborCount`, `isPentagon` を持つ状態を維持してください。
- 五角形セルは `isPentagon === true` を持つ 5 近傍の特殊セルとして扱います。
- 現在の固定メッシュでは、非五角形セルは 6 近傍セルです。
- 現在の惑星モデルでは、一部セルを固定の `water` セルとして扱います。
- 更新ルールは単純平均化ではなく、水場近接・周囲植生・肥沃度・地質を使う局所環境ルールを維持してください。
- メッシュは `createGoldbergMesh(frequency)` で生成します。位相の不変条件は `12` 個の五角形セルと、それ以外が `6` 近傍であることです。

## 実装時の指針

- シミュレーション状態と描画コードの分離を保ってください。
- 現在の挙動が共通でも、将来の表示分岐やルール分岐のために `isPentagon` は残してください。
- メッシュ生成を変更する場合は、隣接関係の双方向性と期待される五角形セル数を維持してください。
- メッシュの位相や更新ルールを変更する場合は、対応するテストの追加や更新を優先してください。

## 検証

挙動や構造に影響する変更を行った場合は、完了前に次を実行してください。

```bash
npm test
npm run build
```

フロントエンドの見た目や操作感に影響する変更を行う場合は、可能ならヘッドレスブラウザで表示や操作を確認してください。通常ブラウザでの最終確認はユーザーが行う前提です。

- `npm run dev` はユーザーがすでに起動していることがあります。まず既存の開発サーバーを利用できるか確認してください。
- 開発サーバーが未起動で、確認に必要な場合のみ `npm run dev` を起動してください。
- ヘッドレスブラウザで画面確認や操作確認ができたかどうか、どの方法で確認したかを完了時に明記してください。
